class Solution:
    def isPalindrome(self, s: str) -> bool:

        s= re.sub(r'[^a-zA-Z0-9]', '', s)

        left = 0
        right = len(s)-1

        if len(s)==0 or len(s)==1:
            return True


        
        s = s.lower()
        while left < right:
            print ( s[left], ord(s[left]), s[right], ord(s[right]))

            while ( not s[left].isalnum()):
                left+=1
                if left >= len(s):
                    return False
            while (not s[right].isalnum()):
                right-=1
                if right < 0:
                    return False

            print ("new", s[left], ord(s[left]), s[right], ord(s[right]) )

            if (s[left] != s[right]):
                print(s[left], s[right])
                return False
        
            left+=1
            right-=1

        return True
